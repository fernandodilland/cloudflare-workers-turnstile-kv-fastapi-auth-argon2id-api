# Python Workers currently support only standard library modules
# External packages like FastAPI are only available in local development
# For production deployment, we use the standard library only
